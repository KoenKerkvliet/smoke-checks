{
  "name": "smoke-checks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only smoke-tests voor WordPress-sites na plugin-updates (Playwright + Supabase + GitHub Actions).",
  "scripts": {
    "smoke": "tsx src/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "playwright": "^1.48.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
